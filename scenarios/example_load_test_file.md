# Load the test file and check ribbon state

A simpler scenario, useful as a smoke test: just load the built-in test
file and click a ribbon button that should be available afterward.

- Load the test file
- Click the Retry Comm button
- Wait 2 seconds
